<h1>My Rentings</h1>

<div class="containerrentings" style= "display: flex">
  <div class="row">
    <div class="col">
      <% @rentings.each do |renting| %>
        <div class="card-renting">
          <%= image_tag "Superpower.png" %>
          <div class="card-renting-infos">
            <div>
              <h2><%= renting.superpower.name %></h2>
              <p><%= renting.start_date %></p>
              <p><%= renting.end_date %></p>
            </div>
            <h2 class="card-renting-pricing">Total price : <%= renting.total_price %>€</h2>
            <p><%= link_to "edit the renting",  edit_renting_path(renting), class: "btn btn-success" %></td>
             <button id="sweet-alert" class="btn btn-danger">Delete the renting</button>
          <%= link_to "", renting_path(renting), method: :delete, id: "delete-link"%>
          </div>
      <% end %>
    </div>
<div class="containerrentings">
<%= link_to "back to homepage",  root_path, class: "btn btn-warning"  %>
</div>


